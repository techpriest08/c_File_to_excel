# c_File_to_excel
<br>This tool maps every line of C source and header files to a corresponding Excel row.
<br>it targets .c and .h extensions.
<br>Note: You must manually enter the target folder path and the output file name. Since this is a script (not an .exe), run it using the python command in the terminal.
<br> this code made by 100% google gemini
